import styled from 'styled-components';
import tw from 'twin.macro';

export const Title = styled.h1`
  ${tw`text-base sm:text-lg md:text-xl lg:text-2xl xl:text-3xl`}
  ${tw`text-purple-600`}
  ${tw`italic sm:not-italic md:italic lg:not-italic xl:italic`}
`;

// export const Logo =
